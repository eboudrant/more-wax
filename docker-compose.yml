services:
  morewax:
    build: .
    env_file: .env
    ports:
      - "8765:8765"
      - "8766:8766"
    volumes:
      - morewax-data:/app/data
    restart: unless-stopped

volumes:
  morewax-data:
